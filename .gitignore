# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.mypy_cache/

# Environment / secrets — NEVER commit
.env
*.key
master.key

# Pleiades + Anamnesis runtime state (profiles, vaults, browser data, model history)
# These live under the user's home dir, but ignore any local copies too.
.pleiades/
.anamnesis/
*.db
*.sqlite
*.sqlite3

# Model weights
*.gguf
models/

# OS
.DS_Store
Thumbs.db

# editor/manual backups + uv (project uses pip/venv, not uv)
*.bak
uv.lock

# vendored/runtime
__pycache__/
*.pyc

# runtime working-memory + model state
memory/
.pleiades/
config.json
models*.json

# statewise worktree build dirs
build-sw/
engine/build-sw/
third_party/llama.cpp/build-sw/

# SearXNG: fetched at install time (pinned commit, not vendored in-repo --
# see pleiades/searxng_runtime.py's module docstring and install.sh's
# install_searxng()). Own venv lives inside it too.
searxng-src/
HANDOFF.md
